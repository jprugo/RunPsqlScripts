directories:
    ScriptsTables:
        schema: public #util
        childs:
            # Hosted directories where the script is executed
            - Install
            - Uninstall
    ScriptsInserts:
        schema: public #util
        childs:
            # Hosted directories where the script is executed
            - Install
            - Uninstall
run:
    2022-01-11 09:39:
        sprint: '21'
        lote: '10'
        creador: Laura
        # folders
        ScriptsTables:
            Install:
                - install.sql
            Uninstall:
                - uninstall.sql
        ScriptsInserts:
            Install:
                - insert.sql
            Uninstall:
                - uninstall.sql
    2022-01-11 10:39:
        sprint: '21'
        lote: '11'
        creador: Laura
        # folders
        # ScriptsTables:
        #     Install:
        #         - install.sql
        #     Uninstall:
        #         - uninstall.sql
        ScriptsInserts:
            Install:
                - insert.sql